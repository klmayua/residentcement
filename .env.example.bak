# Environment Variables

# Application
NODE_ENV=development
PORT=3000

# Admin (Required for database seeding)
ADMIN_PASSWORD=SecureP@ssw0rd!Min12Chars

# Database
POSTGRES_PASSWORD=dev_password_2026
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=resident_cement

# MongoDB
MONGO_PASSWORD=dev_password_2026
MONGO_URI=mongodb://localhost:27017

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# Kafka
KAFKA_BROKERS=localhost:29092

# Keycloak
KEYCLOAK_PASSWORD=admin_password_2026
KEYCLOAK_URL=http://localhost:8180

# JWT (Required - must be at least 32 characters for security)
JWT_SECRET=your-secure-jwt-secret-min-32-chars-change-in-production

# MinIO/S3
MINIO_PASSWORD=minio_password_2026
MINIO_ENDPOINT=localhost
MINIO_PORT=9000

# Frontend
NEXT_PUBLIC_API_URL=http://localhost:3001
NEXT_PUBLIC_KEYCLOAK_URL=http://localhost:8180

# CORS
CORS_ORIGIN=http://localhost:3000

# Logging
LOG_LEVEL=info
