# Step 12: Payment Service only
services:
  payment-service:
    image: residentcement-payment-service:latest
    container_name: residentcement-payment-service
    ports:
      - "3005:3005"
    environment:
      NODE_ENV: production
      PORT: "3005"
      DATABASE_URL: postgresql://resident_cement:xMX2yhYTeTHpvTlkirvDxRKgR-YGJIXU@residentcement-postgres:5432/resident_cement
      KAFKA_BROKERS: residentcement-kafka:29092
      PAYSTACK_SECRET_KEY: ""
    networks:
      - resident-cement-network
    restart: unless-stopped

networks:
  resident-cement-network:
    external: true
    name: residentcement_resident-cement-network
