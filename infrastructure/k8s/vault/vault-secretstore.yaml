apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: vault-backend
spec:
  provider:
    vault:
      server: "http://vault.vault.svc.cluster.local:8200"
      # For production with TLS:
      # server: "https://vault.vault.svc.cluster.local:8200"
      # caBundle: <base64-encoded-ca-bundle>
      path: "secret"
      version: "v2"
      auth:
        kubernetes:
          mountPath: "kubernetes"
          role: "resident-cement"
          serviceAccountRef:
            name: resident-cement
            namespace: resident-cement
---
# Alternative: Token-based authentication (for development)
# apiVersion: external-secrets.io/v1beta1
# kind: SecretStore
# metadata:
#   name: vault-backend
#   namespace: resident-cement
# spec:
#   provider:
#     vault:
#       server: "http://vault.vault.svc.cluster.local:8200"
#       path: "secret"
#       version: "v2"
#       auth:
#         tokenSecretRef:
#           name: vault-token
#           key: token
