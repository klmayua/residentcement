apiVersion: 1

providers:
  - name: 'ResidentCement Dashboards'
    orgId: 1
    folder: 'ResidentCement'
    folderUid: 'resident-cement'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
